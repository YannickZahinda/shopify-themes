
<!doctype html>
<html
  class="no-js{% if request.design_mode %} shopify-design-mode{% endif %}"
  lang="{{ request.locale.iso_code }}"
>
  <head>
    {%- if settings.favicon != blank -%}
      <link
        rel="icon"
        type="image/png"
        href="{{ settings.favicon | image_url: width: 32, height: 32 }}"
      >
    {%- endif -%}

    {% comment %} This a way to wait for main content to load when navigating to a new page so that the view transitions can work consistently {% endcomment %}
    <link
      rel="expect"
      href="#MainContent"
      blocking="render"
      id="view-transition-render-blocker"
    >

    {%- render 'meta-tags' -%}
    {%- render 'stylesheets' -%}
    {%- render 'fonts' -%}
    {%- render 'scripts' -%}
    {%- render 'theme-styles-variables' -%}
    {%- render 'color-schemes' -%}

    {% if request.design_mode %}
      {%- render 'theme-editor' -%}
    {% endif %}

    {{ content_for_header }}
    <style>
  
  
</style>

    <!-- Google Tag Manager -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer','GTM-MHDK554R');</script>
    <!-- End Google Tag Manager -->
    {% render 'sr-checkout' %}
  </head>
  <body class="page-width-{{ settings.page_width }} card-hover-effect-{{ settings.card_hover_effect }}">
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-MHDK554R"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->

    {%- comment -%} Wishlist Drawer {%- endcomment -%}
    <div id="wishlist-drawer" class="wishlist-drawer">
      <div class="wishlist-overlay"></div>
      
      <div class="wishlist-container">
        <div class="wishlist-header">
          <h2>My Wishlist</h2>
          <button class="wishlist-close" aria-label="Close wishlist">
            <svg width="20" height="20" viewBox="0 0 20 20">
              <path d="M15 5L5 15M5 5l10 10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
          </button>
        </div>

        <div class="wishlist-body">
          <div id="wishlist-items"></div>
          <div id="wishlist-empty" class="wishlist-empty" style="display: none;">
            <svg width="60" height="60" viewBox="0 0 60 60" fill="none">
              <path d="M30 54L27 51.3C17 42.36 10 36.28 10 28.5C10 22.42 14.42 18 20.5 18C23.74 18 26.91 19.81 29 22.09C31.09 19.81 34.26 18 37.5 18C43.58 18 48 22.42 48 28.5C48 36.28 41 42.36 31 51.3L30 54Z" stroke="currentColor" stroke-width="2"/>
            </svg>
            <p>Your wishlist is empty</p>
            <a href="/collections/all" class="btn-continue-shopping">Continue Shopping</a>
          </div>
        </div>

        <div class="wishlist-footer">
          <button class="btn-view-all">View All Products</button>
        </div>
      </div>
    </div>

    {% render 'skip-to-content-link', href: '#MainContent', text: 'accessibility.skip_to_text' %}
    <div id="header-group">
      {% sections 'header-group' %}
    </div>

    <script
      src="{{ 'critical.js' | asset_url }}"
      type="module"
      async
      blocking="render"
    ></script>

    <main
      id="MainContent"
      class="content-for-layout"
      role="main"
      data-page-transition-enabled="{{ settings.page_transition_enabled }}"
      data-product-transition="{{ settings.transition_to_main_product }}"
      data-template="{{ template }}"
    >
      {{ content_for_layout }}
    </main>

    {% render 'age-verification-popup' %}

    {% sections 'footer-group' %}

    {% render 'search-modal' %}

    {% if settings.quick_add or settings.mobile_quick_add %}
      {% render 'quick-add-modal' %}
    {% endif %}

    {%- comment -%} Wishlist Drawer JavaScript {%- endcomment -%}
    <script src="{{ 'wishlist-drawer.js' | asset_url }}" defer></script>

    {%- comment -%} Judge.me popup brand styling — MutationObserver overrides web-component JS re-styles {%- endcomment -%}
    <script>
      (function () {
        var CARD_STYLES = {
          background: '#ffffff',
          borderRadius: '20px',
          boxShadow: '0 24px 80px rgba(26,13,18,0.28)',
          padding: '40px 36px',
          maxWidth: '520px',
          width: '92%',
          margin: 'auto',
          border: 'none',
          position: 'relative'
        };
        var OVERLAY_STYLES = {
          background: 'rgba(26,13,18,0.78)',
          backdropFilter: 'blur(6px)',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          padding: '20px'
        };

        function applyPopupStyles() {
          // Overlay (full-screen backdrop)
          var overlays = document.querySelectorAll('.jdgm-overlay, #jdgm-popup-overlay');
          overlays.forEach(function (el) {
            Object.assign(el.style, OVERLAY_STYLES);
          });

          // Card (the white box)
          var cards = document.querySelectorAll('#jdgm-popup, .jdgm-popup, .jdgm-rev-form-wrap');
          cards.forEach(function (el) {
            Object.assign(el.style, CARD_STYLES);
            // Ensure all direct text children are dark
            el.querySelectorAll('p, h1, h2, h3, h4, label, span:not(.jdgm-star)').forEach(function (t) {
              if (!t.classList.contains('jdgm-star') && !t.closest('i.jdgm-star')) {
                t.style.setProperty('color', '#1A0D12', 'important');
                t.style.setProperty('-webkit-text-fill-color', '#1A0D12', 'important');
              }
            });
          });
        }

        // Run immediately and watch for Judge.me injecting the popup
        var observer = new MutationObserver(function (mutations) {
          mutations.forEach(function (m) {
            m.addedNodes.forEach(function (node) {
              if (node.nodeType === 1) {
                var id = node.id || '';
                var cls = node.className || '';
                if (id === 'jdgm-popup' || cls.indexOf('jdgm-popup') !== -1 ||
                    cls.indexOf('jdgm-overlay') !== -1 || cls.indexOf('jdgm-rev-form-wrap') !== -1) {
                  setTimeout(applyPopupStyles, 0);
                }
              }
            });
            // Also catch attribute changes (Judge.me may toggle visibility via style)
            if (m.type === 'attributes' && m.target) {
              var t = m.target;
              if ((t.id === 'jdgm-popup' || (t.className && t.className.indexOf('jdgm') !== -1))) {
                setTimeout(applyPopupStyles, 0);
              }
            }
          });
        });

        document.addEventListener('DOMContentLoaded', function () {
          observer.observe(document.body, { childList: true, subtree: true, attributes: true, attributeFilter: ['style', 'class'] });
          // In case popup already exists
          applyPopupStyles();
        });
      })();
    </script>
  </body>
</html>
